# VITE_API_URL is intentionally left unset.
# config.js automatically uses window.location.hostname:3001,
# which works on both localhost AND any LAN device (phone, tablet, etc.)
# Only set this if you're using a tunnel (e.g. Cloudflare) or a custom domain.
# VITE_API_URL=http://localhost:3001
